import { useGameStore } from "../../GameStore";

export default function Leaderboard() {
  function generateBody() {
    let output = null;

    return output;
  }
  return <div style={{ color: "white" }}>{generateBody()}</div>;
}
